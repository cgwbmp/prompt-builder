## What this changes

<!-- One or two sentences. -->

## Checklist

- [ ] `npm run build` passes locally.
- [ ] No prompt or category `id` was renamed or removed. Share links and saved
      favorites store ids, so a rename breaks them.
- [ ] New prompts match the category `description` in `src/data/categories.ts`,
      and the PR touches one category.
